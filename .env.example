# "Ask about me" chat (see lib/ask-config.ts). Copy to .env.local and fill in.
ANTHROPIC_API_KEY=

# Upstash console -> Search database
UPSTASH_SEARCH_REST_URL=
UPSTASH_SEARCH_REST_TOKEN=

# Upstash console -> Redis database (rate limits, daily cap, answer cache)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
